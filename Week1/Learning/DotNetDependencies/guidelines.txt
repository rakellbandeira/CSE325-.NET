Criar - nova aplicação: dotnet new console -f net8.0
Add package / remove - dotnet add <package> / remmove <package
See inslatlled depend.: dotnet list package
check outdated packages: dotnet list package --outdated

Para debugar usando system Diagnostics:
using System.Diagnostics;

at the star tof the class/method:
Debug.WriteLine($"Entering {nameof(Fibonacci)} method");
Debug.WriteLine($"We are looking for the {n}th number");

at the end:
Debug.WriteLineIf(sum == 1, $"sum is 1, n1 is {n1}, n2 is {n2}");  


Check for conditions with Assert - By using Debug.Assert, you can check for a condition and output additional information about the state of the application.
Ex: 
Debug.Assert(n2 == 5, "The return value is not 5 and it should be.");
return n == 0 ? n1 : n2;

When Debug.Assert is run in the code, the debugger stops the application so you can inspect variables, watch window, call stack, and more. It also outputs the message to the debug console.
Ex:
---- DEBUG ASSERTION FAILED ----
---- Assert Short Message ----
The return value is not 5 and it should be.
---- Assert Long Message ----

If ran, without debug, the app is terminated after the assertion is failed. Output:
Process terminated. Assertion failed.
The return value is not 5 and it should be.
   at Program.<<Main>$>g__Fibonacci|0_0(Int32 n) in C:\Users\Jon\Desktop\DotNetDebugging\Program.cs:line 23
   at Program.<Main>$(String[] args) in C:\Users\Jon\Desktop\DotNetDebugging\Program.cs:line 3



Working with file system - 
1 Include the:
 using System.IO;
 using System.Collections.Generic;
2 List all directories:
- Directory.EnumerateDirectories 
3 List files in a specific directory:
- Directory.EnumerateFiles
Directory.EnumerateFiles(folderName, "*", SearchOption.AllDirectories);

Others: file.EndsWith(" ")


Work with file paths in .NET
1 .NET exposes the full path to the current directory via the Directory.GetCurrentDirectory method.
Console.WriteLine(Directory.GetCurrentDirectory());

Work with paths
- Special path characters:
Console.WriteLine($"stores{Path.DirectorySeparatorChar}201");
- Join paths
Console.WriteLine(Path.Combine("stores","201")); // outputs: stores/201
- Determine filename extensions
Console.WriteLine(Path.GetExtension("sales.json")); // outputs: .jsona